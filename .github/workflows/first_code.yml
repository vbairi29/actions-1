name: My first workflow
on: push
jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
        - name: checkout
          uses: actions/checkout@v4
        - name: Welcome Page
          run: echo "my first step"

        - name: list files
          run: ls -l
        
        - name: readme file
          run: cat README.md